# Model summaries

`summary-20260906.json` is a compact index of the current revalidation set. A future per-model export should follow `../schema.json` and include all seven suite objects; empty objects are preferable to omitted fields when a historical result is not available in the public export.
